try:
    import numpy
except ImportError:
    print "Could not load numpy module, exiting..."
    exit()

try:
    import scipy
except ImportError:
    print "Could not load scipy module, exiting..."
    exit()
